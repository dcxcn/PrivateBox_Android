<?xml version="1.0" encoding="utf-8"?>

<cn.dcx.bookreader.view.CustomLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/pbox_item_root"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    app:backgroundColor="@color/blueTranslucent"
    >
    <ImageView
        android:id="@+id/main_book_item_cover"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:padding="10dp"
        android:src="@drawable/ic_pbox"
        android:visibility="visible"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_toEndOf="@+id/pbox_item_cover">
        <TextView
            android:id="@+id/pbox_item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginTop="15dp"
            android:maxLines="1"
            android:textColor="@color/black"
            android:textSize="18sp"/>
        <TextView
            android:id="@+id/pbox_item_path"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:maxLines="4"
            android:textStyle="italic"
            android:ellipsize="end"
            android:padding="5dp"/>

    </LinearLayout>
</cn.dcx.bookreader.view.CustomLayout>